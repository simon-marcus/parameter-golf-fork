# Top JEPA Examples

- rank 1: exp #35, val_bpb=0.231994, file=./top_examples/rank_01_exp_0035_val_0.231994.py
  desc: Deepen ContextAggregator from 1-layer to 2-layer causal self-attention (each with its own QKV+out_proj and pre-norm residual) — with 3 context patches, a single attention layer ...
- rank 2: exp #20, val_bpb=0.232354, file=(snapshot_missing)
  desc: Add intra-patch self-attention (4-head, bidirectional) in TargetPatchEncoder between the depthwise conv and attention pooling — the conv only captures local byte patterns (kerne...
- rank 3: exp #5, val_bpb=0.234440, file=(snapshot_missing)
  desc: Upgrade CurrentPatchEncoder from simple ReLU+mean-pool to SwiGLU gating + multi-head attention pooling (4 heads), matching the target encoder's expressive pooling mechanism — th...
